# Full-page screenshots are written here by `npm run capture` (Playwright).
# Until then, ProjectCard renders a designed "awaiting capture" plate.
# Filenames must match each project id in src/data/projects.ts.
